// expresion ? 'si es true' : 'si es false';

let edad = 16;
let acceso = edad > 17 ? 'Acceso permitido' : 'Acceso denegado';
console.log(acceso); //Acceso denegado

/**
 * En ester caso este operador funciona muy parecido as un if else, si la condicion es verdadera
 * Ya que podemos asignar un valor a una variable dependiendo de la condicion
 */